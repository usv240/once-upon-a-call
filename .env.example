# --- Vonage (written by setup script) ---
VONAGE_API_KEY=""
VONAGE_API_SECRET=""
VONAGE_APPLICATION_NAME=""
API_APPLICATION_ID=""
COUNTRY_CODE=""
VONAGE_PHONE_NUMBER=""
PRIVATE_KEY64=""

# --- Once Upon a Call ---
# One family per server. Story text uses {child} / {parent} placeholders, so these names are
# spoken in the welcome menu, printed on the parent's card and read out on the last page.
CHILD_NAME=""
PARENT_NAME=""

# Public URL Vonage can reach (auto-derived on GitHub Codespaces; set for ngrok/localtunnel)
PUBLIC_URL=""

# Safety: only these numbers may enter the child's room (comma-separated, digits only).
# Leave empty for open demo mode. Others are asked for FAMILY_PIN (if set).
APPROVED_NUMBERS=""
FAMILY_PIN=""

# Optional: caregiver gets an SMS when a story is saved (US SMS may require 10DLC registration)
CAREGIVER_NUMBER=""

# Optional: live word highlighting (Deepgram streaming ASR). Free tier at deepgram.com
DEEPGRAM_API_KEY=""
